% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/og_app_helpers.R
\name{og_metric_settings}
\alias{og_metric_settings}
\title{Read metric settings from a project's metric workflow YAMLs}
\usage{
og_metric_settings(project_dir)
}
\arguments{
\item{project_dir}{Character. Path to an open.gismo project folder created by
\code{og_init()}.}
}
\value{
A data.frame with class \code{"og_metric_settings"} and one row per metric
YAML, with columns:
\describe{
\item{metric}{Metric ID (from \code{meta$ID}, falling back to the file stem).}
\item{file}{Absolute path to the metric YAML file.}
\item{Active}{Logical; \code{FALSE} only when \code{meta$Active} is explicitly
false.}
\item{Threshold}{Character; the comma-separated \code{meta$Threshold} string,
or \code{NA} when absent.}
\item{GroupLevel}{Character; \code{meta$GroupLevel} (e.g. "Site" or
"Country"), or \code{NA} when absent.}
}
When the metrics directory is missing or empty, a zero-row data.frame with
these columns is returned.
}
\description{
Scans \verb{workflows/2_metrics/*.yaml} inside a project folder and returns a
tidy data.frame of the user-editable settings for each metric. This is the
read half of the thin settings editor in \code{\link[=og_app]{og_app()}}: the metric YAML files
on disk are the single source of truth, so calling this again after an edit
always reflects what will actually run.
}
\details{
The \code{Active} flag follows the package convention (see
\code{\link[=display_activation_status]{display_activation_status()}}): a metric is active unless its \code{meta$Active}
field is explicitly \code{FALSE}, so freshly scaffolded metrics default to active.
}
\examples{
\dontrun{
og_init("~/my-study", example = TRUE)
og_metric_settings("~/my-study")
}

}
\seealso{
\code{\link[=og_metric_settings_update]{og_metric_settings_update()}} for the write half.
}
