% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_project.R
\name{og_project_paths}
\alias{og_project_paths}
\title{Standard paths inside a local-first project folder}
\usage{
og_project_paths(project_dir)
}
\arguments{
\item{project_dir}{Character. Path to the project folder (created by
\code{\link[=og_init]{og_init()}}).}
}
\value{
A named list with elements \code{root}, \code{config}, \code{study_config},
\code{data_config}, \code{packages_config}, \code{workflows}, \code{input}, \code{output},
\code{readme}, \code{index_html}, \code{manifest}, \code{index_json}, and \code{status_json}.
}
\description{
Returns the canonical locations of every part of an open.gismo project
folder so callers never hard-code path fragments. All paths are absolute
(the root is normalized; sub-paths are built from it) but the folder need
not exist yet — this is pure path arithmetic.
}
\examples{
paths <- og_project_paths(tempfile("study"))
basename(paths$data_config)
}
