% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_SaveData.R
\name{fs_SaveData}
\alias{fs_SaveData}
\title{Save workflow results to a local project folder}
\usage{
fs_SaveData(lWorkflow, lConfig)
}
\arguments{
\item{lWorkflow}{List. Workflow object with \verb{$meta}, \verb{$steps}, and
\verb{$lResult}.}

\item{lConfig}{List. Config object from \code{\link[=fs_lConfig]{fs_lConfig()}} with \verb{$project_dir}.}
}
\value{
\code{NULL} (invisible). Side effect: writes CSVs and merges
\code{status.json} on disk.
}
\description{
Writes each data.frame produced by a workflow to
\verb{output/\{phase\}/\{ID\}/\{name\}.csv} under \code{lConfig$project_dir}, where \code{phase}
is derived from \code{lWorkflow$meta$Type} (Mapped -> \verb{1_mappings},
Analysis -> \verb{2_metrics}, Reporting -> \verb{3_reporting}, Report -> \verb{4_modules}).
It then merges an entry for this workflow into \code{status.json} at the project
root, preserving entries written by earlier workflows. The entry carries a
\code{steps} array (\verb{\{name, output, status, error\}} per saved artifact) matching
\code{\link[=og_write_status_json]{og_write_status_json()}}'s shape, so both status.json producers emit the one
shape the bundled site consumes.
}
\details{
\code{lWorkflow$lResult} may be a single data.frame (as produced by
\code{\link[workr:RunWorkflow]{workr::RunWorkflow()}}, named after the final step's output) or a named
list of data.frames; both are handled. Write failures for an individual
artifact are logged via \code{\link[=warning]{warning()}} and skipped so the remaining artifacts
still save.
}
\seealso{
\code{\link[=gh_SaveData]{gh_SaveData()}} for the GitHub-backed twin.
}
