% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_SaveData.R
\name{fs_build_steps}
\alias{fs_build_steps}
\title{Build the status.json \code{steps} array for a saved workflow}
\usage{
fs_build_steps(lWorkflow, saved_names)
}
\arguments{
\item{lWorkflow}{List. Workflow object with \verb{$steps}.}

\item{saved_names}{Character. Names of the artifacts written to disk.}
}
\value{
A list of step entries (empty list when nothing was saved).
}
\description{
Produces one \code{list(name, output, status, error)} entry per saved artifact,
matching the shape \code{\link[=og_write_status_json]{og_write_status_json()}} emits (and the shape the bundled
site's status parser consumes). \code{name} is the workflow step whose \code{output}
matches the artifact, falling back to the final step's name (or \code{"="} when
the workflow declares no steps).
}
\keyword{internal}
