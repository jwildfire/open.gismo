% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/og_run.R
\name{.og_snapshot_date}
\alias{.og_snapshot_date}
\title{The date a snapshot describes, derived from the data cut}
\usage{
.og_snapshot_date(lRaw, override = NULL)
}
\arguments{
\item{lRaw}{Named list of raw input data.frames.}

\item{override}{Optional Date or \code{"YYYY-MM-DD"} string to use instead.}
}
\description{
Takes the newest parseable \code{YYYY-MM-DD} value across every date-named column
of the raw input. That is the point in study time the cut represents, which
is what a reader comparing two snapshots is actually comparing — the wall
clock at the moment the pipeline happened to run says nothing about the data
and makes two cuts of the same study share a date.
}
\details{
Falls back to \code{Sys.Date()} when the input carries no usable date at all.
}
\keyword{internal}
