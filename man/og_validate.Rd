% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/og_validate.R
\name{og_validate}
\alias{og_validate}
\title{Validate a project's input data against its mapping specs}
\usage{
og_validate(project_dir)
}
\arguments{
\item{project_dir}{Character. Path to a project folder created by
\code{\link[=og_init]{og_init()}}.}
}
\value{
A data.frame (with class \code{"og_validation"} and a dedicated
\link[=print.og_validation]{print} method) with one row per input domain and
columns:
\describe{
\item{domain}{Data domain name, e.g. \code{"Raw_AE"}.}
\item{file}{Input file path, relative to the project root.}
\item{status}{One of \code{"ok"}, \code{"warning"}, \code{"error"}, \code{"missing"}.}
\item{n_rows, n_cols}{Dimensions of the file (\code{NA} when unreadable).}
\item{problems}{Human-readable description of any issues (\code{""} if none).}
}
}
\description{
Checks each input data domain the mapping workflows expect and reports, in
plain language, whether the project is ready to run. For every \verb{Raw_*}
domain referenced by \verb{workflows/1_mappings/}, it verifies that the input CSV
exists (per \code{config/data-config.yaml}, defaulting to \verb{input/\{domain\}.csv}),
that the columns the spec requires are present, and that numeric columns
hold coercible values.
}
\details{
This is the "forgiveness layer": it is deliberately lenient about date
formats and extra columns, and it names exactly what is missing so a user
can fix their data before running \code{\link[=og_run]{og_run()}}.
}
\examples{
\dontrun{
og_init("~/my-study", example = TRUE)
og_validate("~/my-study")
}
}
