% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/og_app_helpers.R
\name{og_metric_settings_update}
\alias{og_metric_settings_update}
\title{Update one metric's settings in its workflow YAML}
\usage{
og_metric_settings_update(project_dir, metric_id, values)
}
\arguments{
\item{project_dir}{Character. Path to an open.gismo project folder.}

\item{metric_id}{Character. The metric to update, matched against \code{meta$ID}
(falling back to the YAML file stem).}

\item{values}{Named list of \code{meta} fields to set, e.g.
\code{list(Active = FALSE, Threshold = "-3,-2,2,3", GroupLevel = "Country")}.
An \code{Active} value is coerced to a scalar logical; all other values are
written verbatim. Names not already present in \code{meta} are added.}
}
\value{
Invisibly, the absolute path to the YAML file that was written.
}
\description{
Writes user-edited settings back into a single \verb{workflows/2_metrics/*.yaml}
file, mutating only the named \code{meta} keys and preserving everything else in
the file (the \code{spec} block, the \code{steps}, and every other \code{meta} field). This
is the write half of the thin settings editor in \code{\link[=og_app]{og_app()}}; because state
lives entirely in the folder, the change takes effect on the next
\code{og_run()}.
}
\examples{
\dontrun{
og_metric_settings_update(
  "~/my-study",
  "kri0001",
  list(Active = FALSE, Threshold = "-3,-2,2,3")
)
}

}
\seealso{
\code{\link[=og_metric_settings]{og_metric_settings()}} for the read half.
}
