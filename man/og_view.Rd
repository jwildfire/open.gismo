% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/og_view.R
\name{og_view}
\alias{og_view}
\title{Serve a project folder locally and open the site in a browser}
\usage{
og_view(project_dir, port = NULL)
}
\arguments{
\item{project_dir}{Character. Path to a project folder created by
\code{\link[=og_init]{og_init()}} (and typically populated by \code{\link[=og_run]{og_run()}}).}

\item{port}{Integer or NULL. Port to serve on; \code{NULL} (default) picks a
random free port.}
}
\value{
Invisibly, a list with \code{url}, \code{port}, and \code{server} (the
\code{\link[httpuv:runStaticServer]{httpuv::runStaticServer()}} handle).
}
\description{
Starts a static file server (via \code{\link[httpuv:runStaticServer]{httpuv::runStaticServer()}}) rooted at the
project folder and opens \verb{http://127.0.0.1:\{port\}} in the default browser.
The bundled site page (\code{index.html}) fetches the project's payload files
over HTTP, which is why the folder needs a server rather than a \verb{file://}
open.
}
\details{
The server runs in the background of the current R session: the function
returns (invisibly) the server handle so you can keep working. Stop it with
\code{httpuv::stopServer(server)} or \code{httpuv::stopAllServers()}; it also stops
when the R session ends.
}
\examples{
\dontrun{
viewer <- og_view("~/my-study")
viewer$url
httpuv::stopServer(viewer$server)
}
}
\seealso{
\code{\link[=og_run]{og_run()}} to generate the content being served.
}
