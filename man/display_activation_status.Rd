% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/activation_status.R
\name{display_activation_status}
\alias{display_activation_status}
\title{Display metric activation status before a pipeline run}
\usage{
display_activation_status(lWorkflows, verbose = TRUE)
}
\arguments{
\item{lWorkflows}{Named list of workflow metadata lists.  Each element must
have at minimum an \code{ID} field and may include \code{Active} (logical, default
\code{TRUE}) and \code{GenerateRiskSignal} (logical, default \code{TRUE}) fields.}

\item{verbose}{Logical.  When \code{TRUE} (default), also prints a summary line
with counts of active, inactive, and monitoring-only metrics.}
}
\value{
A data.frame with columns \code{id}, \code{active}, and \code{generate_risk_signal},
invisibly.  The same information is also printed to the console.
}
\description{
Iterates over a list of workflow metadata objects and logs each metric's
\code{Active} and \code{GenerateRiskSignal} state to the console.  Intended to be
called immediately before \code{workr::RunWorkflow()} so operators can confirm
which metrics will run, which are inactive, and which will suppress risk
signal generation.
}
\examples{
workflows <- list(
  list(ID = "kri0001", Active = TRUE,  GenerateRiskSignal = TRUE),
  list(ID = "kri0002", Active = FALSE, GenerateRiskSignal = TRUE),
  list(ID = "kri0003", Active = TRUE,  GenerateRiskSignal = FALSE)
)
display_activation_status(workflows)

}
