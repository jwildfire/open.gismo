% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/og_app.R
\name{og_app}
\alias{og_app}
\title{Launch the thin open.gismo project app}
\usage{
og_app(project_dir = NULL, launch.browser = TRUE)
}
\arguments{
\item{project_dir}{Character or \code{NULL}. Optional path to an existing project
folder to open on launch. When \code{NULL}, the Project panel starts empty and
you point it at (or create) a folder interactively.}

\item{launch.browser}{Logical. Passed to \code{\link[shiny:runApp]{shiny::runApp()}}; when \code{TRUE}
(default) the app opens in a browser.}
}
\value{
This function is called for its side effect (running the app) and
does not return until the app is closed.
}
\description{
Opens a small Shiny/bslib application that drives an open.gismo project
folder through the local-first lifecycle: load data, validate it, customise
metric settings, run the pipeline, and view the resulting reports. It is a
deliberately \strong{thin} shell — it holds no hidden state. Every button reads or
writes files in the project folder, so the same results are reachable from
the headless functions (\code{\link[=og_init]{og_init()}}, \code{\link[=og_validate]{og_validate()}}, \code{\link[=og_run]{og_run()}},
\code{\link[=og_view]{og_view()}}) and vice versa.
}
\details{
The four panels are:
\describe{
\item{Project}{Point at (or create) a project folder, drop in \code{Raw_*.csv}
input files, and see per-domain validation.}
\item{Settings}{Toggle each metric's \code{Active} flag and edit its \code{Threshold}
and \code{GroupLevel}, plus an advanced raw-YAML editor. Edits are written to
the metric workflow YAMLs and take effect on the next run.}
\item{Run}{Validate as a gate, then run the pipeline with a progress bar and
a timing/report-count summary.}
\item{Reports}{Browse the interactive reports and static chart exports that
the last run wrote.}
}

This function requires the \pkg{shiny} and \pkg{bslib} packages (listed under
Suggests); it errors with a clear message if either is missing.
}
\examples{
\dontrun{
og_init("~/my-study", example = TRUE)
og_app("~/my-study")
}

}
\seealso{
\code{\link[=og_init]{og_init()}}, \code{\link[=og_validate]{og_validate()}}, \code{\link[=og_run]{og_run()}}, \code{\link[=og_view]{og_view()}}
}
