% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/StandardizeResults.R
\name{StandardizeResults}
\alias{StandardizeResults}
\title{Standardize analyzed metric results for the reporting phase}
\usage{
StandardizeResults(lAnalyzed)
}
\arguments{
\item{lAnalyzed}{List. Analyzed metric results as returned by running the
\verb{2_metrics} workflows (typically a named list of per-metric lists of
data.frames).}
}
\value{
\code{lAnalyzed} with every \code{GroupID} column coerced to character.
Non-data.frame elements are returned untouched.
}
\description{
Coerces the \code{GroupID} column to character in every data.frame found in
\code{lAnalyzed}, including data.frames nested one level deep (the usual shape:
\code{lAnalyzed$Analysis_kri0001$Analysis_Summary}). Site-level metrics carry
numeric site IDs while country-level metrics carry character country codes;
\code{gsm.reporting::BindResults()} needs one consistent type to row-bind them
into the reporting layer.
}
\details{
This function is registered as a workflow step in
\code{inst/workflows/standardize/standardize.yaml} and executed by \code{\link[=og_run]{og_run()}}
between the metrics and reporting phases, so the coercion is a visible,
auditable part of the pipeline rather than a hidden fix-up.
}
\examples{
lAnalyzed <- list(
  Analysis_kri0001 = list(
    Analysis_Summary = data.frame(GroupID = c(101, 102), Metric = c(1, 2))
  )
)
out <- StandardizeResults(lAnalyzed)
class(out$Analysis_kri0001$Analysis_Summary$GroupID)
}
