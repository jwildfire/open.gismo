% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_LoadData.R
\name{fs_LoadData}
\alias{fs_LoadData}
\title{Load data from a local project folder for a workr workflow}
\usage{
fs_LoadData(lWorkflow, lConfig, lData)
}
\arguments{
\item{lWorkflow}{List. Workflow object with \verb{$meta} and \verb{$spec}.}

\item{lConfig}{List. Config object from \code{\link[=fs_lConfig]{fs_lConfig()}} with \verb{$project_dir}
and \verb{$data_config}.}

\item{lData}{List. Existing data list to populate.}
}
\value{
\code{lData} with additional data.frames loaded per \code{lWorkflow$spec}.
}
\description{
Reads \code{lWorkflow$spec} to determine which data domains are needed. For each
declared domain that is not already present in \code{lData}, resolves an input
path (from \code{lConfig$data_config[[domain]]}, else \verb{input/\{domain\}.csv} under
\code{lConfig$project_dir}), reads it with
\code{read.csv(..., stringsAsFactors = FALSE)}, and adds it to \code{lData}.
}
\details{
Domains already present in \code{lData} are left untouched (upstream results take
precedence). Missing files and read errors are logged via \code{\link[=warning]{warning()}} and
skipped rather than raised, so an absent optional domain never aborts a
workflow.
}
\seealso{
\code{\link[=gh_LoadData]{gh_LoadData()}} for the GitHub-backed twin.
}
