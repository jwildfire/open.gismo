% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_project.R
\name{og_read_data_config}
\alias{og_read_data_config}
\title{Read a project's data-config.yaml into a flat domain -> path map}
\usage{
og_read_data_config(project_dir)
}
\arguments{
\item{project_dir}{Character. Path to the project folder.}
}
\value{
A named list mapping domain name to a path string (relative to the
project root). Empty list if no data-config.yaml is present.
}
\description{
The local-first \code{data-config.yaml} maps each input data domain to the CSV
file that supplies it, relative to the project root, e.g.
\code{Raw_AE: input/Raw_AE.csv}. This reader returns that mapping as a flat named
list so callers can look up a path directly with \code{cfg[[domain]]}.
}
\details{
For resilience it also accepts the older nested form used by the GitHub demo
(a top-level \verb{domains:} key whose values are either path strings or lists
with a \verb{path:} field) and flattens it to the same shape.
}
