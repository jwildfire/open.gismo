% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_lConfig.R
\name{fs_lConfig}
\alias{fs_lConfig}
\title{Create a filesystem-backed lConfig object for workr::RunWorkflow}
\usage{
fs_lConfig(project_dir, data_config = NULL)
}
\arguments{
\item{project_dir}{Character. Path to the project folder (created by
\code{\link[=og_init]{og_init()}}); root for \verb{input/}, \verb{output/}, and \code{status.json}.}

\item{data_config}{List or NULL. Parsed \code{data-config.yaml} mapping domains
to input paths. \code{NULL} (the default) uses the \verb{input/\{domain\}.csv}
convention for every domain.}
}
\value{
A list with \code{project_dir}, \code{data_config}, and \code{LoadData} /
\code{SaveData} functions conforming to the workr lConfig interface.
}
\description{
Returns an \code{lConfig} object whose \code{LoadData}/\code{SaveData} hooks read input
CSVs from and write output CSVs to a local project folder, mirroring
\code{\link[=gh_lConfig]{gh_lConfig()}} but using the filesystem instead of GitHub. Pass the result
as the \code{lConfig} argument to \code{\link[workr:RunWorkflow]{workr::RunWorkflow()}} /
\code{\link[workr:RunWorkflows]{workr::RunWorkflows()}}.
}
\details{
\code{data_config} maps workflow spec domains (e.g. \code{Raw_AE}) to input file
paths. Paths may be absolute or relative to \code{project_dir}. When a domain is
absent from \code{data_config}, \code{\link[=fs_LoadData]{fs_LoadData()}} falls back to the convention
\verb{input/\{domain\}.csv} under \code{project_dir}.

The \code{LoadData}/\code{SaveData} closures satisfy the workr provider contract
(formals \verb{(lWorkflow, lConfig, lData)} and \verb{(lWorkflow, lConfig)}), so they
can also be registered by name with workr:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{workr::register_load_provider("fs", fs_LoadData)
workr::register_save_provider("fs", fs_SaveData)
}\if{html}{\out{</div>}}

After registration, a config could reference the hooks by the string
\code{"fs"}. \code{fs_lConfig()} itself returns the functions directly (like
\code{\link[=gh_lConfig]{gh_lConfig()}}), so no registration is required for normal use.
}
\examples{
\dontrun{
lConfig <- fs_lConfig("~/my-study")
workr::RunWorkflow(lWorkflow = wf, lConfig = lConfig)
}
}
\seealso{
\code{\link[=gh_lConfig]{gh_lConfig()}} for the GitHub-backed twin.
}
