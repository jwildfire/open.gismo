% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/og_run.R
\name{og_run}
\alias{og_run}
\title{Run the open.gismo pipeline in a local project folder}
\usage{
og_run(
  project_dir,
  steps = c("mappings", "metrics", "reporting", "reports"),
  quiet = FALSE,
  open = FALSE,
  snapshot_date = NULL,
  history = TRUE
)
}
\arguments{
\item{project_dir}{Character. Path to a project folder created by
\code{\link[=og_init]{og_init()}}.}

\item{steps}{Character vector. Which pipeline phases to run, in order;
must be a prefix of \code{c("mappings", "metrics", "reporting", "reports")}.
Default: all four.}

\item{quiet}{Logical. Suppress workr's per-step logging and og_run's
progress messages? Default \code{FALSE}.}

\item{open}{Logical. Open the project site (\code{index.html}) — or, if the
site page is absent, the first rendered KRI report — in the browser when
the run finishes? Default \code{FALSE}.}

\item{snapshot_date}{Date or \code{"YYYY-MM-DD"} string. The date this snapshot
describes. Default \code{NULL} derives it from the data cut — the newest date
found in the project's raw input — so re-running an old cut reproduces its
snapshot rather than stamping it with today.}

\item{history}{Logical. Read prior runs' results from \verb{history/} as the
longitudinal input to the reporting phase, and archive this run's results
there afterwards? Default \code{TRUE}. This is what makes \code{Numerator_Change},
\code{Score_Change} and the other change columns appear in \code{Reporting_Results};
with no prior snapshot on disk the reporting phase behaves exactly as it
did before.}
}
\value{
Invisibly, a list with:
\itemize{
\item \code{project_dir} — normalized project root.
\item \code{steps} — the phases that ran.
\item \code{timings} — named numeric, wall-clock seconds per phase (plus
\code{payload} and \code{total}).
\item \code{counts} — named integer: mapped domains, analyzed metrics, reporting
objects, rendered reports, static charts (and static chart failures).
\item \code{reports} — the reports.json payload (list with \code{reports} and
\code{static_charts}), when the reports phase ran.
}
}
\description{
Executes the proven four-phase gsm pipeline against the project created by
\code{\link[=og_init]{og_init()}}:
}
\details{
\enumerate{
\item \strong{mappings} — raw input CSVs -> standardized \verb{Mapped_*} domains
(\verb{workflows/1_mappings}).
\item \strong{metrics} — mapped domains -> per-metric analysis results
(\verb{workflows/2_metrics}), followed by the \code{\link[=StandardizeResults]{StandardizeResults()}}
workflow step that coerces \code{GroupID} to character.
\item \strong{reporting} — analysis results -> the reporting data model
(\verb{workflows/3_reporting}).
\item \strong{reports} — reporting model -> interactive KRI report HTML files plus
static chart PNGs (\verb{workflows/4_modules}).
}

Each phase loads its workflows with \code{\link[workr:MakeWorkflowList]{workr::MakeWorkflowList()}} and runs
them with \code{\link[workr:RunWorkflows]{workr::RunWorkflows()}}. Results are written to
\code{output/{phase}/...} in the demo-branch layout, and the payload files
consumed by the bundled site and \code{\link[=og_app]{og_app()}} (\verb{_index.json}, \code{status.json},
\code{manifest.csv}, \verb{output/4_modules/reports.json}) are regenerated after the
run. If the packaged site page is available, it is copied to the project
root as \code{index.html}.

Because later phases consume earlier phases' in-memory results, \code{steps}
must be a prefix of \code{c("mappings", "metrics", "reporting", "reports")}
(e.g. \code{c("mappings", "metrics")} is valid; \code{"reporting"} alone is not).
}
\examples{
\dontrun{
og_init("~/my-study", example = TRUE)
og_validate("~/my-study")
og_run("~/my-study", open = TRUE)
}
}
\seealso{
\code{\link[=og_init]{og_init()}} to scaffold the project, \code{\link[=og_validate]{og_validate()}} to check
inputs first, \code{\link[=og_view]{og_view()}} to serve the results, \code{\link[=og_app]{og_app()}} for the
interactive shell.
}
