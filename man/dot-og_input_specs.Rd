% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/og_validate.R
\name{.og_input_specs}
\alias{.og_input_specs}
\title{Collect the required input (Raw_*) columns and types from the mapping specs}
\usage{
.og_input_specs(workflows_dir)
}
\arguments{
\item{workflows_dir}{Character. The project's \verb{workflows/} directory.}
}
\value{
A named list: domain -> named list of column -> type string.
}
\description{
Parses every \verb{workflows/1_mappings/*.yaml}, reads each workflow's \code{spec}
block, and keeps the domains a user actually supplies — those named
\verb{Raw_*}. Intermediate domains (\verb{Mapped_*}, \verb{Temp_*}, \verb{Reporting_*}) are
produced by the pipeline, not the user, so they are skipped. Columns are
merged across mappings when a domain appears in more than one spec.
Non-column sentinel keys (names starting with \verb{_}, e.g. gsm.core's \verb{_all}
wildcard marker) are dropped so they are never treated as required columns.
}
\keyword{internal}
