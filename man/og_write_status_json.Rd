% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_payload.R
\name{og_write_status_json}
\alias{og_write_status_json}
\title{Write a project's status.json from workflow YAMLs + output/ contents}
\usage{
og_write_status_json(project_dir)
}
\arguments{
\item{project_dir}{Character. Path to the project folder.}
}
\value{
The status list (as written), invisibly.
}
\description{
R port of build-site.sh's status.json generator, with the same
file-presence semantics: a workflow step counts as \code{completed} when its
output CSV exists under \verb{output/\{phase\}/\{workflow_id\}/}; module workflows
also count HTML reports; workflows with no files on disk are \code{not_run}.
The top-level \code{pipeline_status} is derived from the per-workflow entries:
\code{"completed"} when every workflow completed, \code{"partial"} when some did, and
\code{"not_run"} when none did (so a partial run does not claim to be complete).
}
\keyword{internal}
